import React from 'react';
export default function FundingRangeFilter({ title, children }: any) { return <div className="space-y-2"><div className="text-xs font-semibold text-gray-700">{title}</div>{children}</div>; }
